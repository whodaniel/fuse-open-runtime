["path.join(process.cwd(), 'logs', 'app.log'),\n  path.join(process.cwd(), 'logs', 'error.log'),\n];\n\nconst entries = [];\nfor (const logPath of logPaths) {\n  if (fs.existsSync(logPath)) {\n    const logContent = fs.readFileSync(logPath, 'utf-8');\n    const lines = logContent.split('\n').reverse();\n    for (const line of lines) {\n      if (entries.length >= Number(lines)) break;\n      try {\n        const logEntry = JSON.parse(line);\n        if (level !== 'all' && logEntry.level !== level) continue;\n        entries.push({\n          timestamp: logEntry.timestamp || logEntry.time,\n          level: logEntry.level,\n          message: logEntry.message,\n          service: logEntry.service || logEntry.context,\n        });\n      } catch { continue; }\n    }\n  }\n}\n\nconst logs = {\n  timestamp: new Date().toISOString(),\n  level: level,\n  lines: Number(lines),\n  entries,\n};\n```\n\n### 2. AgencyHub vs TNF Comparison\n**Findings:**\n\nFrom search results, AgencyHub functionality appears to be integrated into TNF:\n\n**AgencyHub Features Found in TNF:**\n- `packages/agent-coordination/` - A2A communication and agent coordination\n- `apps/api/src/modules/agency-hub/` - Agency management endpoints\n- `apps/api/src/modules/agency-hub/controllers/agency.controller.ts` - Agency profile management with:\n  - AgencyProfile interface with branding, features, revenue sharing\n  - Multi-tenant agency creation and management\n  - Agency registration and licensing\n  - Agent limits and user limits per agency\n  - Analytics and stats tracking\n\n**Key AgencyHub Features in TNF:**\n1. **White-label Agencies**: Create custom branded instances\n2. **License Management**: Track agency licenses and status\n3. **Revenue Sharing**: Configure revenue splits between house, investors, and affiliates\n4. **Feature Toggles**: Enable/disable features per agency (marketplace, workflows, A2A, blockchain)\n5. **User Limits**: Control agent and user counts per agency\n6. **Custom Branding**: Logo, colors, domains per agency\n7. **Notifications**: Email, Slack, Discord webhook integration\n\n**Status:** AgencyHub functionality appears to be successfully integrated into TNF as `apps/api/src/modules/agency-hub/`\n\n## Recommendations for LLM Options Refactoring\n\n### Current Architecture Issues:\n1. **Scattered Configuration**: LLM provider config in multiple places (types.ts, config.ts, ProviderService.ts, llm-provider.config.ts)\n2. **Duplicated Models**: SambaNova models defined in both ProviderService and backend config\n3. **Inconsistent Naming**: Some use 'sambanova', others use full names\n\n### Recommended Refactoring:\n\n#### 1. Create Central LLM Configuration Schema\n```typescript\n// packages/llm-config/src/models/provider-config.ts\nexport interface LLMProviderDefinition {\n  id: string;\n  name: string;\n  baseUrl: string;\n  authMode: 'api_key' | 'oauth' | 'bearer';\n  apiFormat: 'openai' | 'anthropic' | 'google' | 'custom';\n  enabled: boolean;\n  models: LLMModelDefinition[];\n}\n\nexport interface LLMModelDefinition {\n  id: string;\n  name: string;\n  providerId: string;\n  apiFormat: string;\n  reasoning: boolean;\n  capabilities: string[];\n  contextWindow: number;\n  maxTokens: number;\n}\n```\n\n#### 2. Single Source of Truth\n```typescript\n// packages/llm-config/src/providers/registry.ts\nexport const PROVIDER_REGISTRY: LLMProviderDefinition[] = [\n  // All providers defined here once\n  // Used by both backend and frontend\n];\n```\n\n#### 3. Unified Provider Service\n```typescript\n// packages/llm-config/src/services/unified-provider.service.ts\nexport class UnifiedLLMProviderService {\n  getProviders(): LLMProviderDefinition[]\n  getModels(providerId: string): LLMModelDefinition[]\n  validateConfig(config: any): boolean\n  generateClientConfig(providerId: string): any\n}\n```\n\n## SambaNova Model Summary\n\n### Available Models Added:\n1. **Meta-Llama-3.1-8B-Instruct** - Fast, 128K context, 4K max tokens\n2. **Meta-Llama-3.1-70B-Instruct** - Balanced, 128K context, 4K max tokens\n3. **Meta-Llama-3.1-405B-Instruct** - Flagship, 128K context, 4K max tokens\n4. **ALLaM-7B-Instruct-preview** - Preview, 128K context, 8K max tokens\n5. **DeepSeek-R1** - Reasoning, 64K context, 4K max tokens\n6. **DeepSeek-R1-Distill-Llama-70B** - Reasoning, 64K context, 4K max tokens\n7. **DeepSeek-V3.1-cb** - Reasoning, 128K context, 8K max tokens\n8. **DeepSeek-V3.2** - Reasoning, 128K context, 8K max tokens\n9. **E5-Mistral-7B-Instruct** - Embeddings/text, 32K context, 4K max tokens\n\n### Performance Notes:\n- SambaNova achieves 200+ tokens/second for 405B model\n- 461 tokens/second for 70B model\n- 998+ tokens/second for 8B model\n- Ideal for agentic AI and multi-agent swarms due to low latency\n\n## Next Steps\n\n1. **Complete mock removal** - Update system logs endpoint\n2. **Implement unified LLM config** - Refactor scattered provider definitions\n3. **Add comprehensive tests** - Test real LLM integration\n4. **Document agency features** - Create documentation for agency-hub module\n5. **Performance testing** - Validate SambaNova integration with real API calls"]