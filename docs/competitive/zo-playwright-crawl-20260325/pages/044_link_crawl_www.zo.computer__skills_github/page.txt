MiniMax 2.7 & Kimi K2.5 are free on Zo Zo · Pricing About Newsletter Blog Resources Download Get started Back to Skills Run in Zo github Interact with GitHub using the `gh` CLI. Use `gh issue`, `gh pr`, `gh run`, and `gh api` for issues, PRs, CI runs, and advanced queries. Clawdbot GitHub Skill Use the gh CLI to interact with GitHub. Always specify --repo owner/repo when not in a git directory, or use URLs directly. Pull Requests Check CI status on a PR: gh pr checks 55 --repo owner/repo Copy List recent workflow runs: gh run list --repo owner/repo --limit 10 Copy View a run and see which steps failed: gh run view <run-id> --repo owner/repo Copy View logs for failed steps only: gh run view <run-id> --repo owner/repo --log-failed Copy API for Advanced Queries The gh api command is useful for accessing data not available through other subcommands. Get PR with specific fields: gh api repos/owner/repo/pulls/55 --jq '.title, .state, .user.login' Copy JSON Output Most commands support --json for structured output. You can use --jq to filter: gh issue list --repo owner/repo --json number,title --jq '.[] | "\(.number): \(.title)"' Copy Preview - Open in Zo to view the full skill. PRODUCT Documentation Blog Newsletter Comparisons Pricing Skills Integrations Channels Models Product Updates INFORMATION FAQ Careers Brand Terms Privacy Help Center Contact Security COMMUNITY Discord LinkedIn X @zocomputer YouTube Testimonials Ambassadors Merch Zo Computer Company