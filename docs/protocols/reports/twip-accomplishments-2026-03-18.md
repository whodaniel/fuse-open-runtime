# TWIP Accomplishments Report (2026-03-18)

Status: Completed (this phase) Scope: TNF relay, backend MCP, schema governance,
capability catalog alignment

## 1) Protocol Foundation Completed

1. Drafted TWIP protocol RFC with canonical identity and envelope contracts.
2. Added TWIP JSON Schemas:
   - `docs/protocols/schemas/twip-identity.schema.json`
   - `docs/protocols/schemas/twip-envelope.schema.json`
3. Added OpenAPI transport stub:
   - `docs/protocols/openapi/twip-v0.1-stub.yaml`
4. Added bridge contracts:
   - `docs/protocols/bridges/twip-to-agent-handoff.yml`
   - `docs/protocols/bridges/twip-to-capability-catalog.yml`

## 2) Relay MCP Capability Surface Completed

1. Added TWIP tools for publish, resolve, revoke, policy evaluate, and
   capability registration.
2. Added TWIP resources for identities, audit log, capability card, and schemas.
3. Added TWIP inventory tooling for holistic terminal visibility:
   - `twip_scan_terminals`
   - `twip_get_inventory`
   - resource `tnf://twip/inventory`
4. Added terminal normalization pipeline (`ps` + `tmux`) into canonical TWIP
   identities.

## 3) Backend MCP and Catalog Alignment Completed

1. Added backend protocol tools:
   - `protocol.twip.register_capability`
   - `protocol.twip.policy_evaluate`
2. Added backend TWIP resources:
   - `fuse://twip/spec`
   - `fuse://twip/capability`
   - `fuse://twip/policy`
   - `fuse://twip/inventory` (mirror resource)
3. Added registry-compatible TWIP capability profile:
   - `config/ai-agents/twip-protocol-bridge.json`
4. Updated registry build path to normalize external capability payloads.

## 4) Governance and CI Completed

1. Added schema validation script:
   - `scripts/validate-protocol-schemas.cjs`
2. Added package script:
   - `validate:protocol-schemas`
3. Added TWIP conformance script and fixtures:
   - `scripts/protocols/twip-conformance.cjs`
   - `docs/protocols/schemas/fixtures/twip/envelope.publish.valid.json`
   - `docs/protocols/schemas/fixtures/twip/envelope.publish.invalid-missing-tenant.json`
4. Added CI gate:
   - `.github/workflows/protocol-schema-gate.yml`

## 5) Safety and Reuse Guidance Completed

1. Added TWIP universalization guidance:
   - `docs/protocols/twip-universalization-playbook.md`
2. Added TWIP operator runbook:
   - `docs/protocols/twip-operator-runbook.md`
3. Added signed-envelope verification and replay protection in relay
   publish/resolve paths.
4. Added relay inventory snapshot persistence:
   - `data/protocols/twip-inventory.snapshot.json` (generated by relay scans)
5. Added reusable envelope signing utility:
   - `scripts/protocols/twip-sign-envelope.cjs`

## 6) Validation Status (Current)

1. `node --check apps/relay-server/src/mcp-server.js` (syntax check) passes.
2. `node scripts/validate-protocol-schemas.cjs` passes.
3. `node scripts/protocols/twip-conformance.cjs` passes:
   - schema fixtures
   - adapter parity
   - policy deny cases
   - signed publish/resolve replay checks
4. `pnpm exec tsc --noEmit -p apps/backend/tsconfig.json` still fails due
   unrelated preexisting errors in:
   - `apps/backend/src/services/identity.service.ts`
   - `apps/backend/src/users/users.service.ts`

## 7) Completed This Phase

1. Backend inventory mirror resource added (`fuse://twip/inventory`).
2. Conformance suite added and wired into CI protocol gate.
3. Signed-envelope + replay protections implemented for TWIP publish/resolve
   operations.
4. Relay ESM runtime blocker fixed (`__dirname` initialization and main-module
   guard).

## 8) Terminal Graph Surface Completed

1. Added API endpoint for holistic graph projection:
   - `GET /api/terminals/graph`
2. Added authorization guardrail:
   - authenticated users can access redacted graph
   - `includeCommands=true` requires admin/system level
3. Added frontend visualization routes:
   - `/visualizations/terminals`
   - `/terminals`
4. Added frontend service/types for reusable terminal graph consumption.

## 9) Production Route Wiring Fix (Post-Deploy)

1. Identified runtime 404 on `https://thenewfuse.com/visualizations/terminals`
   despite feature code existing.
2. Root cause: `ComprehensiveRouter` (active production router) did not include
   terminal graph routes, even though `core.routes.tsx` did.
3. Added terminal graph routes to `ComprehensiveRouter`:
   - `/visualizations/terminals`
   - `/terminals`
4. Added these paths to `isPublicRoute` handling so public rendering/navigation
   behavior matches `/visualizations`.

## 10) API Gateway Routing Hardening (Railway)

1. Confirmed frontend route renders in production, but graph data fetch on
   `/api/terminals/graph` returned 404 at the gateway edge.
2. Added `TerminalsGatewayController` and `TerminalsGatewayModule` in
   `apps/api-gateway` to proxy terminal graph requests to upstream services.
3. Added both legacy and versioned gateway compatibility:
   - `/api/terminals/graph`
   - `/api/v1/terminals/graph`
4. Wired `TerminalsGatewayModule` into `apps/api-gateway/src/app.module.ts`.
