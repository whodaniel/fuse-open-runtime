--- a/src/vscode-extension/src/utilities/getUri.ts
+++ b/src/vscode-extension/src/utilities/getUri.ts
@@ -1,4 +1,5 @@
 import * as vscode from 'vscode';
+import * as path from 'path';
 
 /**
  * A helper function that returns a URI for a file/folder in the webview-ui/build folder.
@@ -29,5 +30,5 @@
  * @returns A URI to the resource.
  */
 export function getUri(webview: vscode.Webview, extensionUri: vscode.Uri, pathList: string[]) {
-    return webview.asWebviewUri(vscode.Uri.joinPath(extensionUri, ...pathList));
+    return webview.asWebviewUri(vscode.Uri.file(path.join(extensionUri.fsPath, ...pathList)));
 }

