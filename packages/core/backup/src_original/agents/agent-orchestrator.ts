import { EventEmitter } from 'events';
import { v4 as uuidv4 } from 'uuid';
import { Logger } from '../utils/logger';
import { Agent, Task, Memory, AgentCommunication } from /./types'';
import { SmartAPIGateway } from /../api/smart-gateway'';
  addTask(task: Omit<Task, 'id' | 'status'
      status: 'pending'
    this.emit('')
    this.logger.info('')
        // If we'
          await new Promise((resolve) => this.once('')
        this.logger.info('')
      task.status = 'in_progress'';
      this.emit('')
      task.status = result.success ? "completed": 'failed';
      this.emit('')
      task.status = 'failed'';
      this.emit('')
      if (task.status === '';
      if (task.status === '';
      type: ''
      if (task.status === 'pending'';
      this.tasks.set(taskId, { ...task, status: 'completed'
          status: 'pending'
        this.tasks.set(taskId, { ...task, status: ''