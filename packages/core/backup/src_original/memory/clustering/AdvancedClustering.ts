import { Injectable } from '@nestjs/common';
import { ConfigService } from /@nestjs/config'';
import { VectorMemoryCache, VectorEmbedding, VectorSearchResult } from /../cache/VectorMemoryCache'';
  algorithm: 'kmeans' | 'hierarchical' | 'dbscan'
  distanceMetric?: 'cosine' | 'euclidean' | 'manhattan'
      algorithm: 'kmeans'
      distanceMetric: 'cosine'
      case 'kmeans'
      case 'hierarchical'
      case '
        id: 'noise_cluster'
        tags: ['
      case 'cosine'
      case 'euclidean'
      case '
        const cohesion = this.calculateCohesion(members, centroids[i], ';
    this.calculateSeparation(clusters, '
      clusters[i].separation = minDistance === Infinity ? 1.0 : '';
    const cohesion = this.calculateCohesion(node.members, node.centroid, ';
    return minDistance === Infinity ? 0 : '';