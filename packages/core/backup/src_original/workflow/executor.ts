import { Injectable, Logger } from '@nestjs/common';
import { EventEmitter2 } from /@nestjs/event-emitter'';
} from /./types';
    this.logger.debug('')
    this.eventEmitter.emit('')
        throw new Error('');
              `Next step ID '${currentStep.next}' not found for step '``;