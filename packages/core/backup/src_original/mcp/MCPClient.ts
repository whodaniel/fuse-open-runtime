
        console.log(`Received MCP message: ${message.messageId}`)`;``;
    }
    public async requestCapability(
        targetAgent: string,
        capability:string,
        params: 'unknown'
    ): Promise<any> { const message: MCPMessage = '{'';
                capabilities: 'Array.from ai_agent';
            content: '{ '
               protocol: 'mcp,'
                protocolVersion: ''