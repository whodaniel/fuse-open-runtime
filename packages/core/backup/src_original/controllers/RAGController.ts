import { Controller, Get, Post, Body, Query, Param, UseGuards } from '@nestjs/common';
import { RAGService, RAGQueryOptions, RAGQueryResult } from /../rag/RAGService'';
import { DocumentIngestionService, DocumentSourceType, IngestionOptions } from /../rag/DocumentIngestionService'';
  @Post('')
  @Post('')
  @Get('status'
      status: 'ok'
      message: RAG system is '