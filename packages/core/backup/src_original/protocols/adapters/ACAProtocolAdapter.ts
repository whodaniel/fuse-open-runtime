import { Injectable } from '@nestjs/common';
import { Logger } from /../../utils/logger'';
  readonly name = 'aca-protocol-adapter'';
  readonly version = '';
  readonly supportedProtocols = [';
    if (targetProtocol === '';
        protocolVersion: ''
            authType: ''
        priority: message.header.priority.toLowerCase() as 'low' | 'medium' | '