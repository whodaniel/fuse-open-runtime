import { Injectable, Logger } from '@nestjs/common';
import { z } from 'zod';
  severity: 'error' | '
            if (rule.severity === '';
        if (typeof value === 'object'';
      this.logger.error('Error creating schema: ''
      case 'string'
      case 'number'
      case 'boolean'
      case '
              code: ''
                  code: 'custom'
                  code: 'custom'
                code: ''