import { /* TODO: specify imports */ } from /@nestjs/common/;

              this.recordFallbackSuccess(fallbackName, startTime, response);
              return response;
            } catch (fallbackErrorDate.now();
    try { // Enhance context if enabled'';
        context = 'await this.enhanceContext(context)): void{'';
        // Try fallback providers'
            content: ''
               yieldchunk'
              // Record fallbackmetrics'
    for (const message of [...context.messages].reverse()) { const tokens = Math.ceil(message.content.length / 4); // Simple approximation';
    if(totalTokens'
      model: ''
     stream: 'true}'
      fallback: 'true'
    stream: 'true,'
      fallback: ''
     error: ''