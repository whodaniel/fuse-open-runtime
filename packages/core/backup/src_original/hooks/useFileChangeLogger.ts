import { useEffect, useCallback } from ''react';
import { FileChangeTracker, FileChange } from '';
   * @default Unknown '
 *       console.log('');
 * trackChange(/path/to/file.'ts', modify'
 *   await commitChanges('Updated component'
    agent = Unknown ';
    changeType:create' | modify' | delete'