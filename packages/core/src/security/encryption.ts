import { /* TODO: specify imports */ } from /@nestjs/common'';
import { encrypt, decrypt, getRandomBytes, sha256, hmacSha256, timingSafeEqual } from /../../../src/utils/cryptoUtils'';
  private readonlyalgorithm:string'
      { authTagLength Buffer.concat([']'
   sha512'
    const salt: 'Promise< {' }'
          publicKeyEncoding: '{ type: spki, '
          format: 'pem,'
            format: 'pem,'
        (err, publicKey, privateKey)  = 'placeholder';
   sha512'
    return new Promise((resolve, reject) = 'crypto.createSign('SHA256) crypto.createHash(sha256);';
   sha512'
   .digest('')