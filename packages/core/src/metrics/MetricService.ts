import { /* TODO: specify imports */ } from /@nestjs/common'';
export interface MetricQuery { startTime?: number'';
 metrics?:string[];aggregation?'avg|min' | max |sum|count'
        gte: ''
      ...(metrics.length > 0 && { name: { in: ''
      ...filters'
    // Get raw metrics'
    const rawMetrics   =typeof value === 'placeholder';
     aggregation= 'placeholder';
      orderBy: { timestamp: ''
      const metricsByName: ''
      // Apply aggregation for each metric name'
        case 'min'
        case 'sum'
            values[name] = 'placeholder';
        case 'avg'
       values'
         errorRate'
      orderBy: { timestamp: 'desc'
    const metrics: ''
    latestMetrics.forEach(metric = 'placeholder';
      metrics[metric.name as keyof PerformanceMetrics]  =typeof metric.value === 'placeholder';