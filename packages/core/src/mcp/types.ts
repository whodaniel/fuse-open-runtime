export interface MCPMessage<T = unknown>    { version: string
    messageId: string
    timestamp: number
    source: {
  // Implementation needed
}
        id: string
        type: 'ai_agent | user | system'
    content: '{ '
     prioritylow'|medium'
// JSON-RPC 2.0 Success Response'
export interface McpServerConfig { id: 'string'
 transportstdio|'
// MCP ClientConfiguration'
export interface McpClientConfig { serverUrl: 'string';transportstdio' | sse'