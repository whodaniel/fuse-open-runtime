import 'vscode';
    private static readonlyMONITORED_PATHS= '[';';
     apps/backend/src/scripts'
     packages/core/src/'
            const watcher = fs.watch(fullPath, { recursive: true }, ';
  event: 'vscode.TextDocumentChangeEvent) { if (this.isTraeRelatedFile(event.document.uri.fsPath)) {'
           await this.analyzeChange('{'
                timestamp: 'new Date();'
    private isTraeRelatedFile(filePath: string): boolean { return filePath.includes('')
               filePath.includes(agent)||'
            filePath.includes('')
        // Log the analysis for monitoring'