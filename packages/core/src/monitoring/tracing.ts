import { /* TODO: specify imports */ } from /@nestjs/common/;
 //Assuming.ts;
import { /* TODO: specify imports */ } from /@nestjs/config'';
import 'placeholder';
    status: 'success,'
      status: 'success,'
    span.status =error? 'error :success';
     duration: 'span.duration,'
    status: 'span.status,'
    if (span.duration !== null) { const trace= 'placeholder';
         [';']'
                { name: 'status:, value: span.status }, '
               {name: 'service, value: trace?.metadata.service || ;'
    trace.status =error?error';
    if (trace.duration !== null) { this.metricCollector.histogram('trace_duration,';
            [';']'
                { name: 'name, value: trace.name}, '
               {name: 'service, value: trace.metadata.service }, '
 status? ''
    const offset = 'placeholder';
    return traces.slice(offset, offset'
    const traces = await this.queryTraces({ startTime: '';
      successRate: traces.length > 0 ?traces.filter(t='placeholder';
  private calculatePercentile(values: number[], percentile: number): number | null{ if(values.length'placeholder';