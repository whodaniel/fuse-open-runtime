import { SmartAPIGateway } from '../api-management/SmartAPIGateway';
        // Log insights every minute'
        // Keep the process running'
     process.on('SIGINT, () = '> { '';
            logger.info('ShuttingdownTraeobservation...)): void { logger.error('Error in Trae observation>{'
    logger.error(Failed to start observation: ''