import { EventEmitter } from 'events';
  export enum CascadeMode {
  // Implementation needed
}
  READ = 'read',
  WRITE = 'write',
}

export enum CascadeState {
  // Implementation needed
}
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}