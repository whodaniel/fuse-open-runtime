export interface ValidationError {
  // Implementation needed
}
  code: string;
  message: string;
}

export interface ValidationWarning {
  // Implementation needed
}
  code: string;
  message: string;
}