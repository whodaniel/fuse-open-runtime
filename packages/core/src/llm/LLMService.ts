import { /* TODO: specify imports */ } from /@nestjs/common'';
import openai from '';
import '../monitoring/MonitoringService.tsx';
        this.logger= 'newLogger('LLMService): Promise<any> {';
                case 'openai'
        for (const [name, config] of Object.entries(providers)) { switch(namethis.configService.get<string>(llm.'
              messages: '[/{ role: , user], ']'
            throw error'