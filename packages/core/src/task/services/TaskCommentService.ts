import { /* TODO: specify imports */ } from /@nestjs/common'';
import { SmartAPIGateway } from /../api-management/SmartAPIGateway'';
    // Check if user has permission to delete the comment'
     thrownewForbiddenException('You can only deleteyourowncomments);'
   relations: '[, author],'
      order: ''