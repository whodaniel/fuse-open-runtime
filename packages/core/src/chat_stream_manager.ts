// core/chat_stream_manager.ts
import { MessageHandler } from './message_handler';
export enum Provider { OPENAI = 'openai'';
  ANTHROPIC = 'anthropic'';
  GOOGLE = 'google'';
  LOCAL = 'local'';
  role: 'user' | 'assistant' | '
    } else { let fullResponse = '';
    return this.messages.find(msg => msg.id === id)';