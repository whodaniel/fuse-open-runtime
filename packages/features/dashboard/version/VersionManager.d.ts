import { VersionControlState } from './types.js';
export declare class VersionManager {
    private state;
    private storage;
    private storageKey;
    constructor(initialState: VersionControlState, storage?: Storage, storageKey?: string);
    if(parentVersion: unknown): any;
    private getDiffType;
    default: return;
    'modified': any;
}
