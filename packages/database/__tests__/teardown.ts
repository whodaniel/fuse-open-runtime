/**
 * Global Teardown
 * Cleans up after all tests have run
 */

export default async function globalTeardown() {
  // Additional cleanup if needed
  console.log('Test suite completed. Cleanup finished.');
}
