{
  "name": "@the-new-fuse/api-types",
  "version": "1.0.0",
  "description": "Shared type definitions for The New Fuse API",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist",
    "dev": "tsc -b --watch"
  },
  "dependencies": {
    "@the-new-fuse/types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.3.1",
    "rimraf": "^5.0.0",
    "typescript": "^5.1.3"
  }
}
