/**
 * Entry point for workspace-level tooling that imports the integration-tests package.
 * Test suites remain under `src/**/*.test.ts(x)`.
 */
export const integrationTestsPackage = {
  name: '@the-new-fuse/integration-tests',
};

