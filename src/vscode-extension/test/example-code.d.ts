/**
 * Example code file for testing AI collaboration features
 */
declare function findDuplicates(array: number[]): number[];
declare class DataProcessor {
    private data;
    constructor(initialData: any[]);
    process(): never[];
    filter(predicate: (item: any) => boolean): any[];
    transform(transformer: (item: any) => any): any[];
}
export { findDuplicates, DataProcessor };
//# sourceMappingURL=example-code.d.ts.map