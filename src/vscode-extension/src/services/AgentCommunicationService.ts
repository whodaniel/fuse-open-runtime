import * as vscode from 'vscode';
export class AgentCommunicationService {
    constructor(private context: vscode.ExtensionContext) {}
    // Add any methods that might be called by CommunicationHubProvider or ChatViewProvider during instantiation or early setup
    public dispose(): void {}
}
