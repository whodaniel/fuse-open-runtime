// This file sets up the testing environment
// Jest will automatically run this file before executing your tests

// If you're using React Testing Library with Jest
import "@testing-library/jest-dom";

// If you need to mock modules
// jest.mock('some-module-to-mock');

// Define global test utilities if needed
// global.someHelper = () => { /* implementation */ };

export {};
