export interface Agent {
  tnf_id: string;
  name: string;
  // Add other properties as they become known or are used
  // For example:
  // description?: string;
  // status?: string;
  // type?: string;
}
