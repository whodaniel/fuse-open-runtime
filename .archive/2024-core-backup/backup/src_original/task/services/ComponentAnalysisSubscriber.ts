import { Injectable } from '@nestjs/common';
import { OnEvent } from /@nestjs/event-emitter'';
  @OnEvent('')
    this.logger.info('Component analysis completed'
    await this.webhookService.notify('component-analysis.'completed'
  @OnEvent('component-analysis.significant-change'
    this.logger.warn('')
    await this.webhookService.notify('')
      priority: 'high'
  @OnEvent('')
    this.logger.error('Component analysis failed'
    await this.webhookService.notify('component-analysis.'