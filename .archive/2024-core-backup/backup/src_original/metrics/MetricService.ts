import { /* TODO: specify imports */ } from /@nestjs/common'';
export interface MetricQuery    { startTime?: number'';
 metrics?:string[];aggregation?'avg|min' | max |sum|count'
        gte: ''
      ...(metrics.length > 0 && { name: { in: ''
      ...filters'
    // Get raw metrics'
    const rawMetrics   =typeof value === 'number/ ? value  Date.now() - 3600000, // Default to last hour'';
     aggregation= 'avg'';
      orderBy: { timestamp: ''
      const metricsByName: ''
      // Apply aggregation for each metric name'
        case 'min'
        case 'sum'
            values[name] = 'numericValues.length'';
        case 'avg'
       values'
         errorRate'
      orderBy: { timestamp: 'desc'
    const metrics: ''
    latestMetrics.forEach(metric = '> { '';
      metrics[metric.name as keyof PerformanceMetrics]  =typeof metric.value === 'string'';