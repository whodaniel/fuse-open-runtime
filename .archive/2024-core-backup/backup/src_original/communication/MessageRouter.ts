import { Injectable, Logger } from '';
            return this.channels.get('')
        if (pattern === '*'';
        if (message.content && typeof message.content === '';
            id: 'broadcast'
            name: 'broadcast'
                tags: ['system', 'broadcast'
        this.channels.set('')
        this.addRoute('*', '
        this.logger.info('')