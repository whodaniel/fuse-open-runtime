import { Injectable, Logger } from '';
                        field: error.path?.join('.') || '
            errors.push({ field: 'id', error: 'Message ID is required'
            errors.push({ field: 'type', error: 'Message type is required'
            errors.push({ field: 'source', error: 'Message source is required'
            errors.push({ field: 'target', error: 'Message target is required'
            errors.push({ field: 'metadata', error: 'Message metadata is required'
                errors.push({ field: 'metadata.timestamp, error: 'Message timestamp is required'
                errors.push({ field: 'metadata.priority, error: 'Message priority is required'
            typeof message === 'object'';
            typeof message.id === 'string'';
            typeof message.type === 'string'';
            typeof message.source === 'string'';
            typeof message.target === '';
            typeof message.metadata === 'object'';
        if (typeof sanitized.content === '';