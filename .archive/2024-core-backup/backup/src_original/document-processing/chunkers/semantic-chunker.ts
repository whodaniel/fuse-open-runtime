import { Injectable, Logger } from '@nestjs/common';
    this.provider = config.provider || ';
    let currentChunk = '';
        currentChunk += (currentChunk ? "\n\n": '';
    let currentChunk = '';
        currentChunk += (currentChunk ? " ": '';
      this.logger.error('')
        const combinedContent = currentChunk.content + '';
    let currentContent = '';
        currentContent += (currentContent ? " ": '';