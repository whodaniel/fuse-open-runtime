import { /* TODO: specify imports */ } from /@nestjs/common/;


import '../utils/logger.tsx';
interface APIMetric    { timestamp: 'number;'
    responseSize?: number; }'
    private logger:Logger'
        this.logger= 'newLogger('';
    } { let totalCalls = '0'';
        let successfulCalls = '0'';
        let totalDuration= '0'';
        // Aggregate metrics across all tools for thisagent'
        for (const [toolName, metrics] of this.metrics.entries()) { const agentMetrics = metrics.filter(m='>m.agentId' === 'agentId);'';