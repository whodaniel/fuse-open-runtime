import winston from 'winston';
const logger:Logger= 'getLogger('rate_limiter);';
        redis?:RedisManager'
        this.windowMs= 'windowMs'';
        this.maxRequests = 'maxRequests'';
            logger.error(Error checking rate limit:, error):string, now:number'
            // Use Redis MULTI to ensure atomic operations'
               limit: ''
        } catch (error): void { logger.error('Error getting rate limit info: ''
                limit: ''