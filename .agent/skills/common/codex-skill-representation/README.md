# Codex Skill Representation In TNF Common

This directory tracks representation of every skill discovered in `/Users/danielgoldberg/.codex/skills`.

- `index.json` is the full coverage report.
- `<skill-name>.json` files are per-skill representation cards.
- `../codex-missing-stubs/` is reserved for future placeholder stubs when Codex skills are absent from TNF.
