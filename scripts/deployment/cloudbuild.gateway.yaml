# TNF Cloud Build configuration for api-gateway
# Builds the api-gateway Docker image and deploys to Cloud Run
substitutions:
  _IMAGE_TAG: 'latest'

steps:
  # Step 1: Build the api-gateway image
  - name: 'gcr.io/cloud-builders/docker'
    args:
    - 'build'
    - '--file'
    - 'Dockerfile.api-gateway'
    - '--tag'
    - 'us-central1-docker.pkg.dev/$PROJECT_ID/tnf-services/api-gateway:${_IMAGE_TAG}'
    - '--progress=plain'
    - '.'
    timeout: '1200s'
    id: 'build-api-gateway'

  # Step 2: Push the image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
    - 'push'
    - 'us-central1-docker.pkg.dev/$PROJECT_ID/tnf-services/api-gateway:${_IMAGE_TAG}'
    waitFor: ['build-api-gateway']
    id: 'push-api-gateway'

  # Step 3: Deploy to Cloud Run
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
    - 'run'
    - 'deploy'
    - 'api-gateway'
    - '--image'
    - 'us-central1-docker.pkg.dev/$PROJECT_ID/tnf-services/api-gateway:${_IMAGE_TAG}'
    - '--region'
    - 'us-central1'
    - '--platform'
    - 'managed'
    - '--allow-unauthenticated'
    - '--port'
    - '8080'
    - '--memory'
    - '1Gi'
    - '--cpu'
    - '1'
    - '--min-instances'
    - '0'
    - '--max-instances'
    - '10'
    - '--set-env-vars'
    - 'NODE_ENV=production'
    waitFor: ['push-api-gateway']
    id: 'deploy-api-gateway'

# Store images in Artifact Registry
images:
  - 'us-central1-docker.pkg.dev/$PROJECT_ID/tnf-services/api-gateway:${_IMAGE_TAG}'

# Build options
options:
  machineType: 'E2_HIGHCPU_8'

timeout: '1800s'
