#!/bin/bash

# Create scripts directory if it doesn't exist
mkdir -p scripts

# Create the start-all.sh script (content should be copied from above)
# The following command assumes you've created the file as shown in the previous step

# Set proper permissions
chmod +x scripts/start-all.sh

echo "Script is ready. Run it with: ./scripts/start-all.sh"
