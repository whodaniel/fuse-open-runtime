name = "openclaw-runtime"
main = "src/index.ts"
compatibility_date = "2026-02-09"

account_id = "db1d3832768b2bbad4c5c5d9dbc63f8b"
workers_dev = true
preview_urls = false

[vars]
ENVIRONMENT = "dev"
SHAREDSTATE_API_BASE = "https://tnf-sharedstate.bizsynth.workers.dev"
# Temporary gateway contract endpoint (replace with container/internal route in Phase 2)
OPENCLAW_GATEWAY_URL = "https://openclaw-gateway.bizsynth.workers.dev/v1/ingress/telegram"

[[services]]
binding = "SHAREDSTATE_SERVICE"
service = "tnf-sharedstate"

# Queues disabled for now (Cloudflare account may require explicit enablement for Queues).
# We will re-enable once Queues are confirmed active.
