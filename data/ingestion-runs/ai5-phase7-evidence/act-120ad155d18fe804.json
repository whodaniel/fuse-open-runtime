{
  "action_id": "act-120ad155d18fe804",
  "title": "[AI5] Configure API Gateway for WebSocket Handling",
  "state": "landed",
  "updatedAt": "2026-06-09T10:33:46Z",
  "summary": "Configured the Nest API gateway to explicitly handle HTTP upgrade requests for TNF WebSocket paths. `/ws` and `/api/ws` now proxy to the relay target, while `/redis-bridge` and `/api/redis-bridge` proxy to the Redis bridge target. Targets are configurable by environment and require no new dependency.",
  "filesChanged": [
    "apps/api-gateway/src/main.ts",
    "data/ingestion-runs/ai5-phase7-evidence/act-120ad155d18fe804.json"
  ],
  "checks": [
    {
      "command": "pnpm --dir apps/api-gateway run type-check",
      "result": "passed"
    },
    {
      "command": "pnpm --dir apps/api-gateway exec tsc -p tsconfig.build.json --noEmit --skipLibCheck",
      "result": "passed"
    },
    {
      "command": "git diff --check -- apps/api-gateway/src/main.ts",
      "result": "passed"
    }
  ],
  "runtime_contract": {
    "relay_paths": ["/ws", "/api/ws"],
    "redis_bridge_paths": ["/redis-bridge", "/api/redis-bridge"],
    "env_overrides": [
      "API_GATEWAY_RELAY_WS_TARGET",
      "API_GATEWAY_WS_TARGET",
      "TNF_RELAY_URL",
      "RELAY_WS_URL",
      "RELAY_URL",
      "API_GATEWAY_REDIS_BRIDGE_WS_TARGET",
      "WS_BRIDGE_PORT"
    ]
  }
}
