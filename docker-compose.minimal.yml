version: '3.8'

services:
  # Frontend service
  frontend:
    build:
      context: .
      dockerfile: apps/frontend/Dockerfile.production
    image: the-new-fuse/frontend:production
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped

  # Redis service
  redis:
    image: redis:7-alpine
    container_name: the-new-fuse-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped

volumes:
  redis_data:

networks:
  default:
    name: the-new-fuse-network
