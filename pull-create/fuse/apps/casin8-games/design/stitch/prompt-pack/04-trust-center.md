Generate a "Provably Fair & Trust Center" UI for Casin8.

Include:

- Compact commit-reveal explainer with clear visual steps.
- Inputs: server seed, client seed, nonce, game, bet, options.
- Output: receipt digest and pass/fail verification result.
- Copy-to-clipboard and export receipt JSON.
- Session ledger with filter/sort and integrity badges.
- Real-time service health chips (API, Table stream, Chain RPC).
- Incident-safe UX: reconnecting banner, retry queue, duplicate-action
  prevention indicator.

Style goal:

- Institution-grade and transparent, not technical clutter.
- Consistent with ai-arcade.xyz fantasy atmosphere at restrained intensity.
