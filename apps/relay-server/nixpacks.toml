[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[variables]
NODE_VERSION = "20"

[phases.install]
cmds = ["cd apps/relay-server && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["cd apps/relay-server && pnpm run build"]

[start]
cmd = "cd apps/relay-server && pnpm start"