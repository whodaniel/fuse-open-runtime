[build]
builder = "nixpacks"
nixpacksConfigPath = "nixpacks.toml"
buildCommand = "cd apps/mcp-servers && pnpm install --frozen-lockfile && pnpm run build"

[deploy]
startCommand = "cd apps/mcp-servers && pnpm start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[variables]
NODE_ENV = "production"
PORT = "3006"