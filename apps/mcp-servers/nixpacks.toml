[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[variables]
NODE_VERSION = "20"

[phases.install]
cmds = ["cd apps/mcp-servers && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["cd apps/mcp-servers && pnpm run build"]

[start]
cmd = "cd apps/mcp-servers && pnpm start"