{
  "$schema": "https://thenewfuse.com/cloud_runtime.schema.json",
  "deploy": {
    "startCommand": "uvicorn cloud_api:app --host 0.0.0.0 --port ${PORT}",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
