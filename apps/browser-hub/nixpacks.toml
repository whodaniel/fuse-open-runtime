[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[variables]
NODE_VERSION = "20"

[phases.install]
cmds = ["cd apps/browser-hub && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["cd apps/browser-hub && pnpm run build"]

[start]
cmd = "cd apps/browser-hub && pnpm start"