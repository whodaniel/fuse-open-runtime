[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[variables]
NODE_VERSION = "20"

[phases.install]
cmds = ["cd apps/frontend && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["cd apps/frontend && pnpm run build"]

[start]
cmd = "cd apps/frontend && vite preview --host 0.0.0.0 --port $PORT"