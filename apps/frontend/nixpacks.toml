[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @the-new-fuse/frontend-app build"]

[start]
cmd = "npx --yes serve dist -p ${PORT:-3000} -s"
