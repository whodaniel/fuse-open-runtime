import React from 'react';
interface BaseLayoutProps {
    children: React.ReactNode;
    pageTitle: string;
    pageDescription?: string;
}
export declare const BaseLayout: React.React.FC<BaseLayoutProps>;
export {};
