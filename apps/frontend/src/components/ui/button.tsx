// Re-export the Button component from the ui-consolidated package
import { Button as UIButton, ButtonProps } from '@the-new-fuse/ui-consolidated';

export const Button = UIButton;
export type { ButtonProps };
