export interface RealtimeEventStreamProps {
    maxEvents?: number;
    showFilters?: boolean;
    showMetrics?: boolean;
    autoScroll?: boolean;
    className?: string;
}
export declare function RealtimeEventStream({ maxEvents, showFilters, showMetrics, autoScroll: _autoScroll, className, }: RealtimeEventStreamProps): import("react/jsx-runtime").JSX.Element;
