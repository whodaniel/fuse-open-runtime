# Poker QA + Simulation Test Matrix

## Scope Mapping

| Requirement                                          | Coverage                                          | Test(s)                                                                                                                                                                                                                 | Notes                                                                                                                     |
| ---------------------------------------------------- | ------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| Rule correctness (cash)                              | Server API integration + engine edge tests        | `server.poker-qa.test.mjs` -> `rule correctness: cash table actions enforce turn and bet constraints`; `swarm/day7-holdem-engine.test.mjs` -> `missed blind on upcoming blind seat is not double-charged as dead blind` | Validates turn/bet constraints and dead-blind accounting edge case.                                                       |
| Rule correctness (Sit&Go)                            | Server API integration + module tests             | `server.poker-qa.test.mjs` -> `rule correctness: SNG + MTT lifecycle endpoints stay consistent`; `swarm/day2.test.mjs`; `swarm/day3.test.mjs`; `swarm/gap-reinforcement.test.mjs`; `core-logic/holdem-engine/*.test.mjs`     | Covers registration, autostart, elimination, completion, payout consistency.                                              |
| Rule correctness (MTT)                               | Server API integration + module tests             | `server.poker-qa.test.mjs` -> `rule correctness: SNG + MTT lifecycle endpoints stay consistent`; `swarm/day3.test.mjs`; `swarm/gap-reinforcement.test.mjs`; `core-logic/holdem-engine/tournament-coverage.test.mjs`          | Covers registration/start/rebalance/final table merge/elimination lifecycle.                                              |
| Concurrency: duplicate actions                       | Server API integration race test                  | `server.poker-qa.test.mjs` -> `concurrency: duplicate actions and stale reconnect action are rejected`                                                                                                                  | Simultaneous same `idempotencyKey` requests: one accepted, one deterministic duplicate rejection.                         |
| Concurrency: reconnect race / stale action rejection | Server API integration + engine cursor guard test | `server.poker-qa.test.mjs` -> `concurrency: duplicate actions and stale reconnect action are rejected`; `swarm/day7-holdem-engine.test.mjs` -> `engine rejects stale replay cursor and allows exact cursor`             | Enforces optimistic concurrency checks (`expectedCursor`/`expectedSeq`) and engine-level stale replay cursor rejection.   |
| Settlement integrity under retries                   | Server API integration                            | `server.poker-qa.test.mjs` -> `settlement integrity: persists across restart and settlement retries are idempotent`                                                                                                     | `/api/table/settle` idempotent retry behavior verified with same key.                                                     |
| Settlement integrity under restart                   | Server API integration restart test               | `server.poker-qa.test.mjs` -> `settlement integrity: persists across restart and settlement retries are idempotent`                                                                                                     | Verifies bankroll/session settlement survives process restart.                                                            |
| Settlement/recovery integrity under v2 restarts      | Server API integration restart test               | `server.v2-core-production.test.mjs` -> `v2 holdem + tournament state persist across restart`                                                                                                                           | Verifies v2 holdem hand state and tournament registration state survive process restart via persisted recovery snapshots. |
| Deterministic replay/auditability across recovery    | Engine recovery test                              | `swarm/day8-core-production.test.mjs` -> `holdem recovery restore preserves replay/audit integrity`                                                                                                                     | Verifies replay log hash-chain and hand audit hash remain stable after snapshot/restore.                                  |
| Fuzz malformed payloads                              | Server API integration fuzz/negative              | `server.poker-qa.test.mjs` -> `fuzz and abuse resistance: malformed payloads and extreme bets`                                                                                                                          | Invalid JSON and oversized bodies return controlled 400 responses; server remains healthy after fuzz traffic.             |
| Extreme bet sizes                                    | Server API integration                            | `server.poker-qa.test.mjs` -> `fuzz and abuse resistance: malformed payloads and extreme bets`                                                                                                                          | Rejects out-of-range/excessive bets cleanly without state corruption.                                                     |
| Tournament-specific betting constraints              | Engine rule test                                  | `swarm/day7-holdem-engine.test.mjs` -> `straddle is rejected in tournament mode`                                                                                                                                        | Ensures cash-only straddle feature is blocked in tournament mode.                                                         |
| Fairness verification endpoint round-trip            | Server API integration                            | `server.poker-qa.test.mjs` -> `fairness verification round-trip works for valid and invalid digests`                                                                                                                    | Commit/rotate/reveal/verify round-trip + digest mismatch path.                                                            |

## CI Integration

- Workflow: `.github/workflows/poker-qa.yml`
- Trigger: pushes to `main` and PRs touching `apps/casin8-games/**`
- Command: `node --test *.test.mjs swarm/*.test.mjs`

## Current Exit Criteria Status

- Zero critical correctness failures: **met** in current suite run.
- High-severity issues fixed or blocked with mitigation: **met** (see
  `docs/poker-bug-list.md`).
