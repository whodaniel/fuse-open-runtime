version: '3.8'

services:
  simplified-frontend:
    build:
      context: .
      dockerfile: Dockerfile.simplified-frontend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
